 

<p>
  <strong>Company:</strong>
  <%= @user.companies.map{|c| c.name}.join(', ') %>
</p>

<p>
  <strong>Email:</strong>
  <%= @user.email %>
</p>

<p>
  <strong>Permission:</strong>
  <%= @user.permission.map{|p| p.sub('can_', '').humanize}.join(', ') %>
</p>

<hr /><%= link_to 'Edit', edit_user_path(@user) %> |
<%= link_to 'Back', users_path %>
